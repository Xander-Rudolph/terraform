name: LLM PR Review

on:
  pull_request:
      types: [opened, synchronize]
  workflow_call:

jobs:
  review-pr:
    uses: xander-rudolph/.github/.github/workflows/ollama-pr.yml@main
    secrets: inherit
